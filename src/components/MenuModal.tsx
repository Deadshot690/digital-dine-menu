// MenuModal is no longer used — the compact list view doesn't need item modals.
// Keeping as a no-op placeholder to avoid breaking any leftover imports.
const MenuModal = () => null;
export default MenuModal;
